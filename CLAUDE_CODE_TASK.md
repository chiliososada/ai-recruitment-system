# Claude Code 自主开发任务：AI 人材招聘匹配系统

## 1. 你的角色与最终使命

你是本项目的首席软件架构师、全栈工程师、AI 工程师、安全工程师和测试负责人。请直接在当前仓库中完成一个可运行、可测试、可部署的“AI 人材招聘匹配系统”MVP。

不要只输出建议、设计稿、伪代码、脚手架或演示页面。你必须读取仓库、修改代码、创建数据库迁移、运行命令、修复失败、补齐测试和文档，并持续工作到本文的全部验收条件真实成立。

## 2. 自主执行原则

1. 先检查现有仓库结构、`git status`、已有 `CLAUDE.md`、README、包管理器、框架、代码规范、测试和 CI；优先沿用现有架构与约定，不做无必要的重写。
2. 若仓库为空，则采用可维护的 TypeScript 单仓或工作区结构，至少包含 React SPA、Node.js API、共享类型、Supabase 迁移/策略及测试。
3. 不等待人工逐步确认。规格缺少普通实现细节时，选择最合理、最小且可扩展的方案，并记录在 `docs/implementation/DECISIONS.md` 后继续。
4. 缺少 Supabase、LLM、邮件、病毒扫描或部署凭据时，不得停工：实现清晰的 provider/adapter 接口、确定性本地 mock、`.env.example` 和真实接入说明，使本地测试仍可完整通过。
5. 不得通过删除、跳过、弱化测试，关闭类型检查，使用 `any` 滥用，硬编码成功结果，或把核心功能改成占位符来“通过”验收。
6. 不覆盖用户未提交的修改；不执行 `git reset --hard`、`git clean -fd`、强制推送、删除无关文件或其他破坏性操作。未经明确授权不要推送、合并或执行真实生产发布。
7. 每个核心功能必须形成端到端纵向切片：数据库/权限 → API/业务逻辑 → 前端状态与页面 → 自动化测试 → 文档。
8. 任何任务只有在代码存在、测试覆盖、命令验证成功并记录证据后才能标为完成。

## 3. 第一次迭代必须完成的事情

在实现功能前，先完成以下仓库内工作，但完成后立即进入编码，不要停在规划阶段：

- 建立或更新 `docs/implementation/REQUIREMENTS.md`：把本提示词拆成带编号的可追踪需求。
- 建立或更新 `docs/implementation/TASKS.md`：原子任务、依赖、状态、验收标准、对应测试；状态只允许 `TODO`、`IN_PROGRESS`、`DONE`、`BLOCKED_EXTERNAL`。
- 建立或更新 `docs/implementation/PROGRESS.md`：每轮记录已完成内容、修改文件、执行命令、结果和下一项任务。
- 建立或更新 `docs/implementation/DECISIONS.md`：记录架构和规格缺口的合理决策。
- 建立或更新 `docs/implementation/VERIFICATION.md`：最终质量门禁的命令、退出码和结果摘要。
- 运行一次现有安装、类型检查、测试和构建，记录基线；已有失败不得被误算为新功能完成。

## 4. 产品范围与角色

系统主要服务两类用户：

- 求职者/技术人员：注册、维护个人资料、上传履历、查看 AI 技能分析、查看匹配职位与职业建议、浏览企业和职位、申请职位、与企业沟通并确认面试。
- 企业招聘人员：注册企业账号、维护企业资料、创建和管理职位、搜索人才、查看 AI 摘要与推荐、比较候选人、安排面试、维护招聘进度、与候选人沟通。

必须实施基于角色和资源归属的授权。仅隐藏按钮不算授权，后端和 Supabase RLS 也必须阻止越权访问。

## 5. 必须实现的核心功能

### FR-01 注册、认证与权限

- 求职者和企业招聘人员均可通过邮箱和密码注册。
- 使用 Supabase Auth；支持邮箱验证流程，并为本地开发提供可测试方案。
- 建立用户 Profile 与角色模型，保护候选人隐私、企业资源和管理操作。
- 登录态恢复、登出、未认证/无权限错误、受保护路由和基本账号设置必须可用。
- 数据库迁移中必须包含必要的 RLS policy，并有跨角色/跨租户越权测试。

### FR-02 履历上传与解析

- React 前端提供拖放及文件选择上传。
- 支持 PDF 和 Word（至少 DOCX）；单个文件最大 10MB。
- 前后端都校验文件大小、扩展名、MIME 和空文件；使用安全、不可预测的存储路径。
- 文件写入 Supabase Storage；实现病毒扫描 adapter。无真实扫描服务时使用明确标记的本地 mock，生产配置不得默认为跳过扫描。
- 上传后自动触发文本提取和结构化解析，并展示处理状态、错误及重试入口。
- 履历内容属于不可信输入：不得让履历中的指令改变系统提示、调用工具或泄露秘密；LLM 输出必须经过 schema 校验。

### FR-03 AI 技能分析与职业建议

- 通过 LLM provider 从履历中抽取至少：技术技能、相关年限、实务等级、项目/经历证据。
- 使用明确的结构化 JSON Schema；解析失败、超时和无效响应必须可重试且可观测。
- 在前端以清晰列表和雷达图等方式展示技能结果；移动端可读，含加载、空、失败状态。
- 生成中长期职业方向和建议学习领域；输出支持当前界面语言。
- 保存模型/提示词版本、生成时间和必要的可追踪元数据，但日志不得记录完整履历或秘密。

### FR-04 企业与职位管理

- 企业用户可创建和编辑企业资料。
- 职位 CRUD 至少包含：职位名称/类别、描述、必需/优先技能、经验要求、年薪范围、工作地点、工作方式、语言要求、公开/非公开状态。
- 只有企业成员可修改其企业和职位；公开列表不得泄露草稿或非公开职位。
- 表单必须有客户端和服务端共享校验，错误信息可国际化。

### FR-05 AI 匹配

- 使用 Supabase PostgreSQL `pgvector` 或等价向量能力保存候选人和职位 embedding，并创建合理索引。
- 先进行向量召回，再结合技能、经验及其他明确规则计算可解释分数；可使用 LLM 做受约束的重排/解释，但 LLM 不得成为不可验证的唯一评分来源。
- 适合度分数必须归一化为 0–100，边界稳定、算法有版本、输入相同可合理复现。
- 履历分析或职位内容变更后，应能重新生成 embedding 与匹配结果，避免陈旧数据。
- 求职者可查看适合自己的职位推荐；企业可查看某职位的候选人排名、分数与简短理由。
- 对评分边界、排序、权限、无候选人、向量服务失败和 LLM 失败建立自动化测试。

### FR-06 人才搜索与详情

- 企业用户可浏览人才列表，按技能、经验年数及合理条件搜索/过滤，并支持分页和排序。
- AI 推荐人才要有明显但可访问的高亮标识。
- 候选人详情仅显示业务允许的数据；原始履历下载、联系方式及敏感字段必须有严格授权。
- 所有筛选和分页在数据量增加时仍使用数据库查询，不得把全部记录拉到浏览器再过滤。

### FR-07 企业与职位浏览

- 求职者可浏览企业列表、企业详情及公开职位。
- 支持按行业、企业规模、所在地搜索/过滤，并支持分页、排序、空状态和错误状态。
- 企业详情必须正确关联其公开职位，不显示其他租户或未公开数据。

### FR-08 直接消息与通知

- 求职者和有权限的企业成员可在平台内建立会话并发送消息。
- 使用 Supabase Realtime 或等价机制更新消息；支持未读状态和基本通知。
- 只有会话成员可读取和写入消息；必须有 RLS/API 授权和负面测试。
- 防止空消息、超长消息、重复提交和基础 XSS；界面含发送中、失败和重试状态。

### FR-09 多语言

- 默认日语，并支持英语、简体中文、繁体中文：`ja`、`en`、`zh-CN`、`zh-TW`。
- UI 文案、表单校验、错误、空状态及主要 AI 输出均可切换语言。
- 不在组件中散落硬编码用户可见文案；建立缺失翻译键检查或相应测试。
- 切换语言后保持路由和当前业务状态，并持久化用户选择。


### FR-10 候选人比较、面试与招聘管理

- 企业用户可将候选人加入 shortlist，并选择多个候选人进行并排比较；至少比较技能、经验、匹配分数、AI 摘要及关键证据。
- 求职者可对公开职位提交申请或表达兴趣；企业只能管理本企业职位对应的申请。
- 提供最小可用的招聘阶段管理，例如：已申请、筛选中、面试、Offer、录用、拒绝；状态变化必须有权限校验和审计时间。
- 企业可提出面试时间、方式和备注，求职者可查看并确认/拒绝；状态变化触发站内通知。
- 候选人比较不得把敏感字段暴露给无权企业；面试及申请数据必须有 API/RLS 越权负面测试。

## 6. 技术与架构要求

- Frontend：React + TypeScript SPA。若仓库已有 React 框架或构建工具则沿用；空仓库可使用 Vite。
- Backend：Node.js + TypeScript。沿用现有框架；空仓库选择轻量、维护活跃并支持 schema/类型的方案。
- Database/BaaS：Supabase Auth、PostgreSQL、Storage、Realtime；向量检索使用 pgvector。
- AI：实现可替换的 LLM 与 embedding provider；生产实现和确定性测试 mock 必须共享同一接口。
- 前后端共享 DTO/schema/枚举，避免接口类型漂移；输入校验应使用仓库已有方案，空仓库可选择 Zod 等成熟方案。
- 提供版本化数据库迁移、种子/fixture、必要索引、RLS、Storage policy 和回滚/恢复说明。
- API 使用一致的错误结构、鉴权、分页、排序和请求关联 ID；生成 OpenAPI 或等价接口文档。
- 提供 `.env.example`，不得提交真实密钥、token、履历或个人信息。

若仓库为空，推荐但不强制采用：

```text
apps/web/              React + TypeScript SPA
apps/api/              Node.js + TypeScript API
packages/shared/       DTO、schema、类型与通用工具
supabase/migrations/   schema、索引、RLS、Storage policy
supabase/seed.sql      非敏感演示数据
docs/implementation/  需求、任务、决策、进度与验证证据
```

## 7. 最低数据模型覆盖

根据现有仓库合理命名，但至少覆盖等价实体与关系：

- 用户 Profile 与角色
- 候选人资料、履历文件、解析任务、技能及候选人技能
- 企业、企业成员
- 职位、职位技能/条件
- embedding 与算法版本
- 匹配结果、分数和解释
- 会话、会话成员、消息、通知
- 申请、shortlist/候选人比较记录、面试安排及招聘阶段历史

必须包含外键、唯一约束、必要索引、时间字段和删除策略。涉及个人信息的表必须明确谁能读写。

## 8. UX 与可靠性要求

- 所有核心页面支持桌面和移动端，具备明确的加载、空数据、成功、失败和重试状态。
- 表单支持键盘操作、语义化标签、基本对比度和可访问名称；雷达图必须有文本等价信息。
- API 具备超时、统一错误映射、有限重试和速率限制；避免无限重试和重复副作用。
- 日志不得输出密码、token、完整履历或完整 LLM prompt 中的个人信息。
- 对外部 AI 的输出进行 schema 校验和内容边界处理；对履历/职位文本实施 prompt-injection 防护。
- 查询使用分页和索引；避免 N+1、全表拉取和前端秘密泄露。

## 9. 自动化测试与质量门禁

沿用仓库已有工具；空仓库可采用 Vitest/Jest、API 集成测试和 Playwright。至少包含：

- 单元测试：校验、权限、技能解析 schema、评分归一化与排序、i18n。
- 数据库/RLS 测试：求职者、企业成员、其他企业、匿名用户的允许与拒绝场景。
- API 集成测试：认证、上传、解析状态、职位 CRUD、列表筛选、匹配、消息。
- 前端组件/交互测试：关键表单、上传、分析展示、筛选、语言切换、错误状态。
- E2E 主路径：
  1. 求职者注册 → 上传履历 → 查看技能分析/职业建议 → 查看职位推荐与企业 → 发送消息。
  2. 企业注册 → 建立企业 → 创建并公开职位 → 搜索/比较人才 → 查看匹配排名 → 管理申请与安排面试 → 发送消息。
- 文件边界测试：错误类型、超过 10MB、空文件和恶意文件名。
- 安全负面测试：IDOR/跨租户访问、未认证访问、消息越权、未公开职位泄露。

最终必须根据仓库实际脚本执行并通过所有适用门禁：

```text
依赖安装或锁文件一致性检查
格式检查
git diff --check
lint
TypeScript typecheck
单元测试
集成测试
RLS/数据库测试
E2E 测试
生产构建
迁移验证
```

不得只运行新增测试；完成前必须运行全套测试。若某项确实不适用，必须在 `VERIFICATION.md` 写出可审计理由，不可静默跳过。

## 10. 文档与交付物

- README：架构、先决条件、安装、环境变量、Supabase 启动/迁移、开发、测试、构建和部署步骤。
- API 文档：端点、认证、请求/响应、错误和分页。
- AI 文档：provider、结构化输出、embedding、评分公式/权重、算法版本、失败回退和隐私边界。
- 数据库文档：主要表、关系、RLS 和 Storage policy。
- 部署文档：前端、Node API、Supabase、环境变量、迁移顺序、健康检查和回滚。
- 非敏感演示数据或 seed，使评审者能走通两条 E2E 主路径。

## 11. 强制执行循环

在每一轮中严格执行：

1. 读取 `TASKS.md`、`PROGRESS.md`、失败日志和当前 `git diff`。
2. 选择最高优先级、未完成且未阻塞的最小纵向任务，标为 `IN_PROGRESS`。
3. 实现真实功能，并同步数据库、共享类型、API、UI 和必要文档。
4. 先写或补测试，再运行最小相关测试；失败就定位根因并修复，直到通过。
5. 定期运行更大范围门禁，处理回归、类型错误、lint、构建和迁移问题。
6. 做一次自我代码审查：权限、数据泄露、错误处理、并发/重复请求、可维护性和用户体验。
7. 仅在验收证据真实存在后把任务标为 `DONE`，并更新 `PROGRESS.md` 与 `VERIFICATION.md`。
8. 立即进入下一轮。不要因为某一个模块完成、上下文变长或已经输出总结而结束。

若连续两轮没有实质进展，必须改变策略：重新检查根因、缩小复现、阅读相关源码/文档、增加诊断或替换实现；禁止重复同一无效动作。

## 12. 完成条件

只有同时满足以下条件，才允许结束：

- `TASKS.md` 中没有未完成的核心需求，且需求追踪表均指向真实代码和测试。
- FR-01 至 FR-10 均有可运行的端到端实现，不是静态 mock UI、空 handler、伪数据硬编码或 TODO。
- 数据库迁移、索引、RLS、Storage policy 和 pgvector 匹配实现存在且验证通过。
- React 页面、Node API、Supabase 集成、AI provider、四个 locale、消息/通知全部连通。
- 全套适用质量门禁退出码为 0，并记录在 `VERIFICATION.md`。
- 没有为了通过而跳过/删除测试，没有未解释的核心 TODO/FIXME，没有提交秘密或真实个人信息。
- README 和架构、API、AI、数据库、部署文档足以让新开发者从零运行项目。
- 最终执行一次 `git status --short` 与 `git diff --check`，审阅所有改动，确认没有误改无关文件。

满足后，在最终回复中输出：

```text
FINAL_STATUS: COMPLETE
```

并提供简短的“需求编号 → 实现位置 → 测试/验证证据”摘要。只要任一条件未满足，就不得输出 `FINAL_STATUS: COMPLETE`，而应继续下一轮工作。
